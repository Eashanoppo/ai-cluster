SECRET_KEY=django-insecure-change-me-to-something-secure
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration
# If DATABASE_URL is left commented, Django will automatically use a local SQLite database (db.sqlite3).
# DATABASE_URL=postgres://neuronops:password@localhost:5432/neuronops_db

# LLM / Ollama Configuration (Optional)
# If Ollama is offline or not installed, the processor will use deterministic mock logs.
# OLLAMA_BASE_URL=http://localhost:11434/v1
# LLM_MODEL=llama3.1:8b
